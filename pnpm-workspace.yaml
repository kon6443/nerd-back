packages:
  - apps/*

# 패키지별 lockfile — 각 앱이 자급자족해야 Dockerfile 컨텍스트를 apps/<x> 로 좁힐 수 있고,
# 한 앱의 의존성 변경이 다른 앱의 배포를 트리거하지 않는다 (docs/tasks/tasks-monorepo.md D2).
# 공유 패키지(packages/*)가 생기면 재검토한다.
sharedWorkspaceLockfile: false

# 프론트(Next.js)의 sharp · unrs-resolver 는 prebuilt 를 쓰므로 postinstall 이 필요 없다.
# 워크스페이스에서는 루트 설정만 읽힌다 (원래 apps/front/pnpm-workspace.yaml 에 있던 값).
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver

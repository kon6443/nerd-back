# dependencies
node_modules/
.pnp
.pnp.js

# build
dist/
build/
*.tsbuildinfo

# env
.env
.env.*
!.env.example
!.env.migration.example

# logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# test
coverage/
.nyc_output/

# editor / os
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.swp

# runtime
*.pid
*.seed
tmp/

# 세션 스냅샷 (PreCompact 훅 생성 — 정본 아님, docs/tasks/ 로 옮긴 뒤 삭제)
**/docs/handoff/

# 설정 롤백용 백업 사본 — 커밋 금지 (방치되면 어느 쪽이 정본인지 흐려진다)
*.bak
*.bak-*


# Next.js (apps/front) — 앵커 없이 두어 어느 앱 디렉터리에서든 매칭되게 한다
.next/
out/
next-env.d.ts
.vercel
*.pem

# pnpm 이 전역 store 를 못 쓸 때 만드는 폴백 store — 커밋 금지
.pnpm-store/
